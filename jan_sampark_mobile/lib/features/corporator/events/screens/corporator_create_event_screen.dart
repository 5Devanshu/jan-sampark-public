// Corporator create event delegates entirely to the Leader
// create event screen — same form, same endpoint,
// area is resolved server-side from the Corporator's token.
export '../../../leader/events/screens/leader_create_event_screen.dart';