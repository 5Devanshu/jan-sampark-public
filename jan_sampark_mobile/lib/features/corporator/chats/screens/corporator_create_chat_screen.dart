// Corporator create chat reuses Leader's screen.
// The only difference is the backend uses the Corporator's
// area_id instead of a ward_id — transparent to the UI.
export '../../../leader/chats/screens/leader_create_chat_screen.dart';